#include<iostream>
#include <unordered_map>
#include<vector>
using namespace std; 

#define DBG(x, v) cout << "#" << x << " " << v << endl;
#define FOR(i, x) for (i = 0; i  < x; i++)
#define ALL(arr) (arr.begin(), arr.end())

/*
text = ahishers     => n 
arr[] = {"he", "she", "hers", "his"}   => k
O(n + m + z) => z is the total number of occurrences of a word. 
*/ 

/*



*/

class Solution {
public:
    vector<int> findSubstring(string s, vector<string>& words) {
    }
};
